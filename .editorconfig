root = true

# Unix-style newlines with a newline ending every file
[*]
end_of_line = lf
insert_final_newline = true
charset = utf-8

# Also, on all file types, use two spaces to indent
indent_style = space
indent_size = 2
